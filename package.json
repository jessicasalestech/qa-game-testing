{
  "name": "qa-game-testing",
  "version": "1.0.0",
  "description": "QA Portfolio — game test automation. Unit tests of the game logic with Jest + E2E tests of the real game in the browser with Playwright.",
  "author": "Jessica Sales",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=20.10"
  },
  "scripts": {
    "test:unit": "jest",
    "test:e2e": "playwright test",
    "test": "npm run test:unit && npm run test:e2e",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "jest": "^29.7.0"
  }
}